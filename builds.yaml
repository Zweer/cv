# Which CVs to build: person × theme combinations
# person must match a filename in data/ (without .yaml)
# theme must match a folder in themes/

builds:
  - person: nic
    theme: formentera
  - person: ro
    theme: budapest
